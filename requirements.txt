# Core data / ML
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
lightgbm==4.3.0
catboost==1.2.10
joblib==1.4.2
pyarrow==16.0.0
shap==0.46.0

# Schema robustness layer (§A)
pandera==0.32.1
rapidfuzz==3.14.5

# Hierarchical reconciliation (§E)
hierarchicalforecast==1.5.1

# LLM causal-summary layer (§H) — optional at runtime; the pipeline falls
# falls back to a deterministic rule-based narrator if this or an API key
# is unavailable (see src/llm_insights.py)
anthropic==0.116.0
groq==1.5.0

# Frontend (§I)
streamlit==1.58.0
plotly==6.8.0

# Testing
pytest==8.3.3
